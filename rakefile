require 'rake_tasks'

require 'cane'

require 'rake_tasks/tasks/cane'
require 'rake_tasks/tasks/spec'

$VERBOSE = true

task :default => []
Rake::Task[:default].clear_prerequisites

task :default => [
  :cane,
  :specs,
]
