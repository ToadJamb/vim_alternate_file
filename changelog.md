Changelog
=========

0.0.3
-----

* Use global functions.
	`af:OpenAlternate` and `alternate_file:OpenAlternate`
	change to `OpenAlternateFile`
* There are no deprecation warnings about function name changes,
	as the previous (scoped) form of functions is what this update
	is 'fixing' for some configurations.
